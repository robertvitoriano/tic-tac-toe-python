class Player:

    def __init__(self, name, symbol, color,):
        self.name = name
        self.symbol = symbol
        self.color = color


def define_symbols(self):
    self.symbol = input()
    if(self.symbol.capitalize == "X"):
        player2_character = "Y"
    else:
        player2_character = "X"
